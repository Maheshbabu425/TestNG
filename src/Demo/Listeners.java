package Demo;

import org.testng.ITestListener;


// ITestListener interface which implements Testing Listeners
// implements key word which is used to invoke interface
public class Listeners implements ITestListener {

	
}
